name: manual tag test
on:
  push:
    branches:
      - main

jobs:
  tag-version:
    runs-on: ubuntu-latest
    steps:
      # - uses: actions/checkout@v3
        # with:
          # fetch-depth: 0
      - run: git tag "test-tag-manual"
